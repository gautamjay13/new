# Frontend: set this when deploying. Point to your backend URL (no trailing slash).
# Example for production: https://fraud-watch-api.onrender.com
# Leave unset for local dev (defaults to http://localhost:3001)
VITE_API_URL=
